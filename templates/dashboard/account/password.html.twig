{% extends 'dashboard/account/_layout.html.twig' %}

{% block title %}{{ 'account.security_settings.password.title'|trans }}{% endblock %}

{% block main %}
    <twig:Header title="{{ 'account.security_settings.password.title'|trans }}">
        <a href="{{ path('dashboard.account') }}" class="button button-danger">
            {{ 'account.security_settings.password.cancel'|trans }}
        </a>
    </twig:Header>

    <twig:Card>
        <twig:CardBody>
            {{ form(form) }}
        </twig:CardBody>
    </twig:Card>
{% endblock %}