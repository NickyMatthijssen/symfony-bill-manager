{% extends 'authentication/_authentication.html.twig' %}

{% block title %}{{ 'authentication.sign_up.title'|trans }}{% endblock %}

{% block main %}
    {{ form(form) }}

    <p class="mt-10 text-center text-sm/6 text-gray-500">
        {{ 'authentication.sign_up.account_exists'|trans }}
        <a href="{{ path('authentication.sign-in') }}" class="font-semibold text-indigo-400 hover:text-indigo-300">
            {{ 'authentication.sign_up.sign_in_here'|trans }}
        </a>
    </p>
{% endblock %}