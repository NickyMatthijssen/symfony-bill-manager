<div{{ attributes }} class="px-4 sm:px-6 py-5 border-b border-b-gray-800">
    <div class="-ml-4 -mt-2 flex justify-between flex-nowrap items-center">
        <div class="ml-4 mt-2">
            <h3 class="text-white font-semibold text-base">
                {{ title }}
            </h3>
        </div>

        <div class="ml-4 mt-2 flex justify-end space-x-4">
            {% block action %}{% endblock %}
        </div>
    </div>
</div>
